DROP TABLE IF EXISTS Member2;

CREATE TABLE Member2
(
    id          IDENTITY        PRIMARY KEY,
    name        VARCHAR(255)    NOT NULL,
    password    VARCHAR(255)    NOT NULL,
    email       VARCHAR(255)
);

CREATE TABLE STUDENT
(
    STU_NO          VARCHAR(255) PRIMARY KEY,
    STU_NAME        VARCHAR(255),   
    STU_DEPT    	VARCHAR(255),
    STU_GRADE       VARCHAR(255),
    STU_WEIGHT		VARCHAR(255),
    STU_HEIGHT		VARCHAR(255)
);
INSERT INTO  STUDENT  VALUES('20221234','홍길동','컴퓨터학과','1', '60', '180');

CREATE TABLE ENROL
(
    STU_NO          VARCHAR(255),
    ENR_GRADE        VARCHAR(255),   
    SUB_NO	    	VARCHAR(255)
);
INSERT INTO  ENROL  VALUES('20221234', '80', '1001');
CREATE TABLE SUBJECT
(
    SUB_NO          VARCHAR(255) PRIMARY KEY,
    SUB_NAME        VARCHAR(255),   
    SUB_DEPT    	VARCHAR(255),
    SUB_PRO       VARCHAR(255)
);
INSERT INTO  SUBJECT  VALUES('1001', '자바', '컴퓨터학과' ,'강호동');


SELECT *
FROM STUDENT S
INNER JOIN ENROL E ON S.STU_NO = E.STU_NO
INNER JOIN SUBJECT SUB ON E.SUB_NO = SUB.SUB_NO;