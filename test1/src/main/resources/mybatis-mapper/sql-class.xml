<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.ClassMapper">
	<select id="classList" parameterType="hashmap" resultType="com.example.test1.model.Class">
		select * from tbl_class
	</select>


	<insert id="classJoin" parameterType="hashmap">
		insert into tbl_participant (userid, class_no, join_date, count) values (#{userId}, #{classNo}, #{joinDate}, 1)
	</insert>
</mapper>	